# Copia questo file in `.env` e inserisci i valori reali.
# `.env` e' escluso da git: i segreti non devono mai stare qui dentro.

PORT=3000

# Apertura su internet.
# Metti true SOLO quando il sito e' gia' raggiungibile con https://, cioe'
# quando davanti c'e' un proxy o un tunnel che fornisce il lucchetto.
# Attivato prima, il sito rimanda a un indirizzo sicuro che non esiste ancora.
SITO_HTTPS=true
HOST_AMMESSI=studio.tuodominio.it
SITO_URL=

# Quanti proxy stanno davanti al sito. Il tunnel Cloudflare conta come 1.
#
# Serve a sapere da dove prendere l'indirizzo di chi chiama, e da quello
# dipendono i freni anti-abuso.
#   0 = nessun proxy: si crede solo alla connessione (locale o rete di studio)
#   1 = dietro il tunnel: l'indirizzo vero lo scrive il proxy
#
# Lasciarlo a 1 senza un proxy davanti e' un buco: l'intestazione con
# l'indirizzo se la scrive chi chiama, e i freni si aggirano cambiandola.
PROXY_DAVANTI=0

# Account amministratore: e' l'indirizzo con cui si entra nel pannello.
ADMIN_EMAIL=auslvaleria@gmail.com
# Password in chiaro SOLO al primo avvio: il server la trasforma in hash
# e la salva nel database. Dopo il primo avvio puoi rimuoverla da .env.
ADMIN_PASSWORD=
ADMIN_PASSWORD_RESET=
SEGRETARIA_PASSWORD_RESET=

# Chiave per firmare i token di sessione.
# Generala con: openssl rand -hex 32
SESSION_SECRET=

# Dove arrivano gli avvisi allo studio: prenotazioni nuove, richieste,
# consegne che faticano.
#
# Ha una riga sua e non segue piu' ADMIN_EMAIL. Erano la stessa cosa, ma
# non lo sono: spostare la posta su un'altra casella voleva dire cambiare
# anche le credenziali con cui si entra nel pannello. Se questa riga manca
# si torna a ADMIN_EMAIL, come prima.
NOTIFY_EMAIL=auslvaleria@gmail.com

# Casella usata per inviare le email ai pazienti, e relativa password.
# Con Gmail: password per le app (16 caratteri) da https://myaccount.google.com/apppasswords
EMAIL_USER=auslvaleria@gmail.com
EMAIL_PASS=

# Server SMTP. Se lo lasci vuoto si usa Gmail, come prima.
# Per far partire le email da un indirizzo del dominio dello studio (così non
# finiscono nello spam dei pazienti), usa la casella del dominio su Aruba:
#   SMTP_HOST=smtps.aruba.it
#   SMTP_PORT=465
#   SMTP_SECURE=true
#   EMAIL_USER=noreply@studiomedicobragliavaleria.it
#   EMAIL_PASS=<password di quella casella>
#   MAIL_FROM=noreply@studiomedicobragliavaleria.it
SMTP_HOST=
SMTP_PORT=465
SMTP_SECURE=true
# Indirizzo che il paziente vede come mittente. Vuoto = uguale a EMAIL_USER.
MAIL_FROM=

# Dove finiscono le copie di sicurezza giornaliere.
#
# Lasciata vuota, le copie stanno accanto all'archivio, in data/backup. Comodo,
# ma se salta quel disco saltano insieme l'originale e tutte le copie. Puntandola
# a una cartella sincronizzata le copie escono dalla macchina, che e' il punto.
#
# Su Windows, per esempio:
#   CARTELLA_BACKUP=C:\Users\valer\OneDrive\StudioMedico\backup
#
# Attenzione: dentro ci sono dati sanitari di persone reali. Sincronizzarli
# significa metterli su un servizio esterno, ed e' una decisione da prendere
# sapendolo, non una comodita' da attivare di sfuggita.
CARTELLA_BACKUP=

# Chiave per cifrare le copie di sicurezza (AES-256-GCM). Senza, i backup
# restano in chiaro e il programma lo segnala a ogni avvio. Generala UNA volta:
#   openssl rand -hex 32
# e conservala fuori dalla macchina e fuori dalla cartella dei backup: senza
# la chiave un backup cifrato non si recupera.
# Per rileggere un backup:  node strumenti/decifra-backup.mjs <file.sqlite.enc>
BACKUP_ENCRYPTION_KEY=
